<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jibba_000.recycleforum">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SÅ GO&apos; SOM NY"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateUserActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="SÅ GO&apos; SOM NY" />
        <activity
            android:name=".PostedActivity"
            android:label="Opslag" />
        <activity
            android:name=".ShowSofabordActivity"
            android:label="HJÆLP MIT SOFABORD" />
        <activity
            android:name=".MyPageActivity"
            android:label="Min side"/>

        <activity android:name=".CreatePostActivity"></activity>
    </application>

</manifest>